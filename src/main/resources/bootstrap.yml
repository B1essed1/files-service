spring:
  application:
    name: ${SERVICE_NAME:file-service}
  profiles:
    active: ${SERVICE_PROFILE:local}
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:165.232.122.8}:${CONFIG_SERVER_PORT:50123}
      username: ${CONFIG_SERVER_USERNAME:hd9kdkmci}
      password: ${CONFIG_SERVER_PASSWORD:LKd7hdkl7jd}
      name: general, ${spring.application.name}